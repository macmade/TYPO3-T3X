<!--

// Script state
script_ended = 1;

// Checks the parent window
if( top.fsMod ) {
    
    // Adds the current page ID to the list of the recent IDs
    top.fsMod.recentIds[ 'web' ] = SOBE.getVar( 'id' );
}

//-->
